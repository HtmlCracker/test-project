spring:
  application:
    name: file-storage-service
  cloud:
    vault:
      uri: http://vault:8200
      token: root
      kv:
        enabled: true
        backend: secret
        application-name: services/file-storage-service,database/fileStorageDb
      fail-fast: true