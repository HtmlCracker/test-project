spring:
  application:
    name: email-sender-service
  cloud:
    vault:
      uri: http://vault:8200
      token: root
      kv:
        enabled: true
        backend: secret
        application-name: services/email-service
      fail-fast: true